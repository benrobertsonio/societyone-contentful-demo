import React from 'react';
import PropTypes from 'prop-types';

const Calculator = ({ title, subtitle }) => (

  <div>
    <h2>{title}</h2>
    <p>{subtitle}</p>
    {/* calculator code goes here */}
    <div>whatever</div>
  </div>
);



export default Calculator;